import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const Otp: React.FC = () => {
  return (
    <View>
      <Text>Otp</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Otp;