import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const Mail: React.FC = () => {
  return (
    <View>
      <Text>Mail</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Mail;