import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const Send: React.FC = () => {
  return (
    <View>
      <Text>Send</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Send;
