import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const Home: React.FC = () => {
  return (
    <View>
      <Text>Home</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Home;
