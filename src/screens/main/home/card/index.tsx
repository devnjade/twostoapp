import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const Card: React.FC = () => {
  return (
    <View>
      <Text>Card</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Card;
